package Model.Enemies;

import Model.Enemy;

public class Cyclops extends Enemy {

    public Cyclops(int x, int y, int speed, float health, float damage) {
        super(x, y, 32, 32, speed, health, damage);
    }
}
